- Các bước tạo bản ghe DKIM 
  -  Vào `configuration`-> `Domain` -> `edit` domain  muốn tạo DKIM
  - Tại tab Genaral -> `DoaminKeys Indentified Mail` -> `show publickey` để lấy thông tin giá trị và tên bản ghi DKIM
  